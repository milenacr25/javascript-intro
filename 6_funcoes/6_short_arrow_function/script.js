dobro = (x) => {
    return x * 2; //utilizado quando quer armazenar o valor, 
    //se só fosse utilizado uma vez poderia colocar o console.log
}
console.log(dobro (2));

triplo = (x) => x * 3;

console.log(triplo(4));
